let person = {
	name: "Shane",
	age: 20,
	country: "Canada",
};

const logData = () => {
	console.log(
		`${person.name} is ${person.age} years old and lives in ${person.country}.`
	);
};

logData();
